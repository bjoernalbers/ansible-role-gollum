#!/usr/bin/env ruby
require 'gollum/app'
require 'gollum/auth'
require 'yaml'

{% if gollum_auth %}
# Enable Authentication
use Gollum::Auth,
  YAML.load_file('{{ gollum_auth_users_path }}'),
  YAML.load_file('{{ gollum_auth_options_path }}').symbolize_keys
{% endif %}

Precious::App.set(:gollum_path, '{{ gollum_repo }}')
Precious::App.set(:default_markup, :markdown)
Precious::App.set(:wiki_options,
                  YAML.load_file('{{ gollum_options_path }}').symbolize_keys)
run Precious::App
